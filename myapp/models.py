from django.db import models
#Aca se crean las clases que se convierten en las tablas sql , como django ya tiene orm integrado el se encarga de crear automaticamente esas tablas y de actualizarlas. En ese momento crea archivos que se guardan en la carpeta migrations. 
# Create your models here.
